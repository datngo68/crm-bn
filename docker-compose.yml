services:
  web:
    build: .
    container_name: crm-bn-web
    ports:
      - "127.0.0.1:3010:3000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - docker_default

networks:
  docker_default:
    external: true
